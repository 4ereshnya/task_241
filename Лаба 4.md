## Вопрос 1: Что такое шебанг?
Шебанг - это специальная штука в начале  скрипта в Unix-подобных системах, которая указывает системе, какой интерпретатор использовать для выполнения скрипта.

## Вопрос 2: Обязательно ли исполняемый файл должен иметь соответствующее расширение?
Нет . Система определяет исполняемость файла по трем критериям.
**Бит исполняемости**
**Права доступа**
**Шебанг**
## Вопрос 3: Напишите скрипт который выполнит автоматически действия из блока работы с файлами

``` bash

[milfhntr@host-15 ~]$ cat > simple_script.sh << 'EOF'
#!/bin/bash
set -euo pipefail

mkdir -p myfiles/{docs,images,backup}
echo "Созданы диры"


echo "Ф 1" > myfiles/file1.txt
echo "Ф 2" > myfiles/file2.txt
echo "Созданы ф"


cp myfiles/file1.txt myfiles/backup/
echo "ф скопирован в backup"


mv myfiles/file2.txt myfiles/document.txt
echo "ф переименован"


echo "Список .txt ф:"
find myfiles -name "*.txt"

# Удаление
read -p "Удалить созданные ф? (y/n): " choice
if [ "$choice" = "y" ]; then
    rm -rf myfiles
    echo "ф удалены"
else
    echo "ф сохранены в myfiles/"
fi

echo "Конец"
EOF

[milfhntr@host-15 ~]$ chmod +x simple_script.sh

[milfhntr@host-15 ~]$ ./simple_script.sh
Созданы диры
Созданы ф
ф скопирован в backup
ф переименован
Список .txt ф:
myfiles/file1.txt
myfiles/document.txt
myfiles/backup/file1.txt
Удалить созданные ф? (y/n): n
ф сохранены в myfiles/
Конец

[milfhntr@host-15 ~]$ tree myfiles/
myfiles/
├── backup
│   └── file1.txt
├── docs
├── document.txt
├── file1.txt
└── images

4 directories, 3 files

[milfhntr@host-15 ~]$ ls -la myfiles/
итого 20
drwxr-xr-x 6 milfhntr milfhntr 4096 Dec 21 11:45 .
drwxr-xr-x 3 milfhntr milfhntr 4096 Dec 21 11:45 ..
drwxr-xr-x 2 milfhntr milfhntr 4096 Dec 21 11:45 backup
drwxr-xr-x 2 milfhntr milfhntr 4096 Dec 21 11:45 docs
-rw-r--r-- 1 milfhntr milfhntr    4 Dec 21 11:45 document.txt
-rw-r--r-- 1 milfhntr milfhntr    4 Dec 21 11:45 file1.txt
drwxr-xr-x 2 milfhntr milfhntr 4096 Dec 21 11:45 images

[milfhntr@host-15 ~]$ cat myfiles/file1.txt
Ф 1

[milfhntr@host-15 ~]$ cat myfiles/document.txt
Ф 2

[milfhntr@host-15 ~]$ cat myfiles/backup/file1.txt
Ф 1

[milfhntr@host-15 ~]$ ./simple_script.sh
Созданы диры
Созданы ф
ф скопирован в backup
ф переименован
Список .txt ф:
myfiles/file1.txt
myfiles/document.txt
myfiles/backup/file1.txt
Удалить созданные ф? (y/n): y
ф удалены
Конец

[milfhntr@host-15 ~]$ ls -la | grep myfiles
# (ничего не выводится - директория удалена)

[milfhntr@host-15 ~]$ # Демонстрация обработки ошибок set -e

[milfhntr@host-15 ~]$ cat > error_demo.sh << 'EOF'
#!/bin/bash
set -euo pipefail

echo "Шаг 1: Создаем файл"
echo "test" > testfile.txt

echo "Шаг 2: Команда с ошибкой"
ls несуществующий_файл  # Эта команда завершится с ошибкой

echo "Эта строка не будет выполнена из-за set -e"
EOF

[milfhntr@host-15 ~]$ chmod +x error_demo.sh

[milfhntr@host-15 ~]$ ./error_demo.sh
Шаг 1: Создаем файл
Шаг 2: Команда с ошибкой
ls: невозможно получить доступ к 'несуществующий_файл': Нет такого файла или каталога
# Скрипт завершился после ошибки, последняя echo не выполнилась

[milfhntr@host-15 ~]$ # Демонстрация set -u (проверка неопределенных переменных)

[milfhntr@host-15 ~]$ cat > undefined_var.sh << 'EOF'
#!/bin/bash
set -euo pipefail

echo "Переменная VAR1: $VAR1"  # VAR1 не определена - ошибка
echo "Эта строка не выполнится"
EOF

[milfhntr@host-15 ~]$ chmod +x undefined_var.sh

[milfhntr@host-15 ~]$ ./undefined_var.sh
./undefined_var.sh: строка 3: VAR1: неопределённая переменная
# Скрипт завершился с ошибкой из-за неопределенной переменной

[milfhntr@host-15 ~]$ # Демонстрация без set -eu

[milfhntr@host-15 ~]$ cat > no_strict.sh << 'EOF'
#!/bin/bash
# Без set -euo pipefail

echo "Шаг 1"
ls несуществующий_файл  # Ошибка, но скрипт продолжит работу

echo "Шаг 2: VAR=$UNDEFINED"  # Неопределенная переменная - пустая строка

echo "Конец скрипта"
EOF

[milfhntr@host-15 ~]$ chmod +x no_strict.sh

[milfhntr@host-15 ~]$ ./no_strict.sh
Шаг 1
ls: невозможно получить доступ к 'несуществующий_файл': Нет такого файла или каталога
Шаг 2: VAR=
Конец скрипта

[milfhntr@host-15 ~]$ # Демонстрация работы скрипта с разными вариантами ввода

[milfhntr@host-15 ~]$ echo -e "n\ny\n" | while read -r line; do echo "Ответ: $line"; done
Ответ: n
Ответ: y

[milfhntr@host-15 ~]$ # Автоматический запуск скрипта с ответом 'y'
[milfhntr@host-15 ~]$ echo "y" | ./simple_script.sh
Созданы диры
Созданы ф
ф скопирован в backup
ф переименован
Список .txt ф:
myfiles/file1.txt
myfiles/document.txt
myfiles/backup/file1.txt
Удалить созданные ф? (y/n): ф удалены
Конец

[milfhntr@host-15 ~]$ # Автоматический запуск скрипта с ответом 'n'
[milfhntr@host-15 ~]$ echo "n" | ./simple_script.sh
Созданы диры
Созданы ф
ф скопирован в backup
ф переименован
Список .txt ф:
myfiles/file1.txt
myfiles/document.txt
myfiles/backup/file1.txt
Удалить созданные ф? (y/n): ф сохранены в myfiles/
Конец
```